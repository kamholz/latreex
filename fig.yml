latreex:
  build: .
  ports:
    - "3001:3000"
  volumes:
    - .:/src
  volumes_from:
    - trees

cron:
  build: cron
  volumes_from:
    - trees

trees:
  image: debian:wheezy
  command: echo data-only container for latreex
  volumes:
    - /trees

texlive:
  build: texlive
